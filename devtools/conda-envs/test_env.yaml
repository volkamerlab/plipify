name: test
channels:
  - conda-forge
  - defaults
  - openbabel
dependencies:
  # Base depends
  - python 2.7
  - pip
  - pandas
  - tqdm
  - openbabel 2.4
  - numpy
  - lxml
  - jupyter
  - notebook
  - nglview
    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
      - https://github.com/ssalentin/plip/archive/stable.zip
