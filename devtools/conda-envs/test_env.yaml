name: test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip
  - pandas
  - tqdm
  - numpy
  - jupyterlab
  - nglview
  - plip
  - matplotlib-base
  - seaborn
    # Testing
  - pytest
  - pytest-cov
