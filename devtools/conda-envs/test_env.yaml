name: test
channels:
  - conda-forge
  - defaults
  - openbabel
dependencies:
  # Base depends
  - python 2.7
  - pip
  - pandas
  - tqdm
  - openbabel 2.4.1
  - numpy
  - lxml
  - future
  - jupyter
  - notebook
  - nglview
  - matplotlib
  - plotly
  - seaborn
  - ipywidgets
    # Testing
  - pytest
  - pytest-cov


  # Pip-only installs
  - pip:
    # needs to be installed manually with --no-deps
    # - https://github.com/ssalentin/plip/archive/stable.zip
